import * as React from "react"
import { Typography } from "@mui/material";

export default function Projects() {
    return(
        <Typography>
            Projects
        </Typography>
    );
    
}